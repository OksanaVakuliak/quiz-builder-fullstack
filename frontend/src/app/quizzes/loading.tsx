import { AppLoader } from '@/components/ui/AppLoader';

export default function QuizzesLoadingPage() {
  return <AppLoader title="Loading quizzes..." subtitle="Preparing list and latest updates" />;
}
